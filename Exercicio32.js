/**
 * Crie um array com 5 elementos;
 * Faça uma iteração entre todos eles e imprima no console o valor;
 */

let arr = ["Pedro", 31, 'Desenvolvedor', true, 9];

for(let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}