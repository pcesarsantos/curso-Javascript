/*
    Armazene a velocidade de um carro em uma variável, com o número que desejar;
    Faça uma estrutura if/else que verifica see ele está acima da velocidade;
    80 é a velocidade máxima permitida;
    se eeestiver acima ou abaixo exiba mensagens com console.log();
*/

let velocidade = 70;

if(velocidade > 80) {
    console.log(`Acima da velocidade permitida, Multado!`);
} else {
    console.log(`Velocidade permitida`);
}